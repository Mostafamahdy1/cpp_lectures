#include <cstdio>

int main()
{
    std::printf("Enter a list of numbers (0 to end): ");
    
    

    while (true)
    {
        std::scanf("%d", &num);
        if (num == 0)
        {
            break;
        }
        
    }
    
}